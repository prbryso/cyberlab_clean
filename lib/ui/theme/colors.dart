import 'package:flutter/material.dart';

class AppColors {
  // Primary brand colors
  static const Color primary = Color(0xFF00C2D1); // Neon teal
  static const Color primaryDark = Color(0xFF008A97);
  static const Color primaryLight = Color(0xFFB6F7FF);

  // Secondary colors
  static const Color cyberBlue = Color(0xFF4A6CF7);
  static const Color softPurple = Color(0xFFB8A6FF);

  // Backgrounds
  static const Color background = Color(0xFFF7F9FB);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceAlt = Color(0xFFF0F3F6);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF4A4A4A);
  static const Color textMuted = Color(0xFF8A8A8A);

  // Accents
  static const Color accent = Color(0xFF00F0FF);
  static const Color success = Color(0xFF2ECC71);
  static const Color warning = Color(0xFFFFC107);
  static const Color danger = Color(0xFFE74C3C);

  // Borders
  static const Color border = Color(0xFFE1E5EA);
}
